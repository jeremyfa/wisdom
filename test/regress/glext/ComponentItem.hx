package glext;

extern class ComponentItem extends ContentItem {
    var componentType(default, never):Dynamic;
    var container(default, never):ComponentContainer;
    var title(default, never):String;
}
