package glext;

extern class Header {
    var element(default, never):js.html.Element;
    var controlsContainerElement(default, never):js.html.Element;
    var tabsContainerElement(default, never):js.html.Element;
}
