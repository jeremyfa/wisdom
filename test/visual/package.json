{
  "name": "wisdom-visual",
  "private": true,
  "description": "Visual check of the Golden Layout integration in a real Chromium: geometry assertions, tab drag & drop, screenshots to inspect.",
  "scripts": {
    "build": "esbuild out/app.js --bundle --format=iife --outfile=out/bundle.js",
    "check": "node check.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.12",
    "golden-layout": "2.6.0",
    "playwright": "1.62.1",
    "tslib": "2.8.1"
  }
}
